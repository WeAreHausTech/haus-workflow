<!-- HAUS-PRERELEASE-CLEANUP: P4e — plugin/ directory removed; surviving skill content relocates to library/global/skills/ in P5. -->
# Setup Modes

Guided mode:

- asks plain-language questions
- better for unclear repo shape

Fast mode:

- scan and recommend defaults only
- better for experienced users

Both modes must confirm before `haus apply --write`.