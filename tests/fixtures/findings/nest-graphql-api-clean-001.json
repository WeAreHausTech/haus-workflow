{
  "id": "nest-graphql-api-clean-001",
  "repo": "nest-graphql-api",
  "status": "clean",
  "commands": [
    "haus scan --json",
    "haus recommend --json",
    "haus context --task \"add graphql resolver with auth guard\" --json"
  ],
  "notes": "Recommendation selects nestjs-graphql-patterns + typescript6-patterns + package-manager + baselines only. No frontend or unrelated stack bleed. Task context correctly narrows to nestjs-graphql-patterns."
}
