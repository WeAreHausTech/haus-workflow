{
  "id": "vendure-monorepo-clean-001",
  "repo": "vendure-monorepo",
  "status": "clean",
  "commands": [
    "haus scan --json",
    "haus recommend --json",
    "haus context --task \"build shipping plugin\" --json",
    "haus context --task \"add admin ui extension\" --json",
    "haus context --task \"create graphql resolver\" --json"
  ],
  "notes": "Selected rules align with backend Vendure/NestJS/GraphQL stack. No frontend bleed. Task router cleanly narrowed `build shipping plugin` to vendure-plugin-patterns and `create graphql resolver` to nestjs-graphql-patterns. `add admin ui extension` returned the full Vendure backend set, which is acceptable since the recommender currently has no Vendure admin-UI (Angular) rule."
}
