---
description: Sole fixture file for the orphan-only set (ADR-0024) — zero manifest items exist, so a disk-orphan failure is the ONLY possible signal. Isolates disk-orphan parity from every other audit category. Not a real skill.
---

# Fixture orphan-solo skill
