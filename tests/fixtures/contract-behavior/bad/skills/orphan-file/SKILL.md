---
description: Deliberately orphaned fixture file (ADR-0024) — on disk, claimed by no manifest item. Not a real skill.
---

# Fixture orphan-file skill

This file has no corresponding item in `manifest.json` — it exists to exercise the
disk-orphan audit on both validators.
