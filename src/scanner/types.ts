/** Result returned by scanProject. */
import type { ContextMap } from '../types.js'

/**
 * Full result returned by {@link scanProject} — the scanned ContextMap, which is
 * also what gets written to context-map.json.
 */
export type ScanResult = ContextMap
